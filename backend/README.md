# Backend (Spring Boot)

This folder will contain the Java Spring Boot backend for the Natural Language Date Interpreter web app.
# gradle --version
# gradle wrapper --gradle-version 8.7
# .\gradlew clean
# .\gradlew bootJar